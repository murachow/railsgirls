<h1>Listing textiles</h1>

<table>
  <thead>
    <tr>
      <th>Size</th>
      <th>Material</th>
      <th>Picture</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @textiles.each do |textile| %>
      <tr>
        <td><%= textile.size %></td>
        <td><%= textile.material %></td>
        <td><%= textile.picture %></td>
        <td><%= textile.name %></td>
        <td><%= link_to 'Show', textile %></td>
        <td><%= link_to 'Edit', edit_textile_path(textile) %></td>
        <td><%= link_to 'Destroy', textile, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Textile', new_textile_path %>
